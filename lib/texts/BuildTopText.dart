import 'package:flutter/material.dart';

Widget buildText() => const Positioned(
      top: 307,
      child: Text(
        "Grace A",
        style: TextStyle(
            color: Colors.black, fontSize: 25, fontWeight: FontWeight.w500),
      ),
    );
